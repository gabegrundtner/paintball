 <tr id="<%= tournament_id_helper(@tournament) %>">
    <td><%= @tournament.date %></td>
    <td><%= @tournament.location %></td>
    <td><%= link_to 'View Tournament', @tournament %></td>
    <td><%= link_to 'Destroy', @tournament, confirm: 'Are you sure?', method: :delete %></td>
  </tr>