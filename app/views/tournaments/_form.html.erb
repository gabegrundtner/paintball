<%= form_for(@tournament) do |f| %>
  <% if @tournament.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tournament.errors.count, "error") %> prohibited this tournament from being saved:</h2>

      <ul>
      <% @tournament.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br />
    <%= f.text_field :date %>
  </div>
  <div class="field">
    <%= f.label :location %><br />
    <%= f.text_field :location %>
  </div>
  <div class="field">
    <p>Add Participants</p>
    <%= link_to 'Participants', new_participants_path %>
  </div>
  <div class="field">
    <p>Add Results</p>
    <%= link_to 'Results', new_results_path %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
